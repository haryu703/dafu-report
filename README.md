# dafu-report
## Usage
```
USAGE:
    dafu-report [OPTIONS] --url <URL>

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

OPTIONS:
        --min <MIN>    表示するチケットの最低価格 [default: 0]
        --url <URL>    検索画面のURL
```
